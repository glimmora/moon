services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: moon
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-moon}
      POSTGRES_DB: moonfun
    volumes:
      - db-data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U moon -d moonfun"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:4000/health',r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 15s
    environment:
      NODE_ENV: production
      BACKEND_PORT: 4000
      DB_URL: postgresql://moon:${POSTGRES_PASSWORD:-moon}@db:5432/moonfun
      AUTH_JWT_SECRET: ${AUTH_JWT_SECRET:?AUTH_JWT_SECRET is required}
      BACKEND_CORS_ORIGIN: ${BACKEND_CORS_ORIGIN:-http://localhost:8080}
    ports:
      - "4000:4000"
    # Apply pending migrations on boot, then start the server.
    command: sh -c "npx prisma migrate deploy && node dist/index.js"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        FRONTEND_API_URL: ${FRONTEND_API_URL:-http://localhost:4000}
        FRONTEND_WALLETCONNECT_PROJECT_ID: ${FRONTEND_WALLETCONNECT_PROJECT_ID:-}
        CHAIN_ETHEREUM_SEPOLIA_RPC_URL: ${CHAIN_ETHEREUM_SEPOLIA_RPC_URL:-https://sepolia.drpc.org}
        CHAIN_FACTORY_ETHEREUM_SEPOLIA: ${CHAIN_FACTORY_ETHEREUM_SEPOLIA:-0xC3DadD2643a6aB9857880EF7Bf208dEdd31937b3}
        CHAIN_FEE_ROUTER_ETHEREUM_SEPOLIA: ${CHAIN_FEE_ROUTER_ETHEREUM_SEPOLIA:-0x95032e828144707e9754993e421c31dE986A3bb1}
        CHAIN_CREATOR_FEE_VAULT_ETHEREUM_SEPOLIA: ${CHAIN_CREATOR_FEE_VAULT_ETHEREUM_SEPOLIA:-0x3c67d2f9f3aA5B909332f2eF7a3862b58015345B}
        CHAIN_REFERRAL_REGISTRY_ETHEREUM_SEPOLIA: ${CHAIN_REFERRAL_REGISTRY_ETHEREUM_SEPOLIA:-0xADB082E1AA4696bffDAD8aB754874d31E37e9Fe0}
        CHAIN_MOON_BURNER_ETHEREUM_SEPOLIA: ${CHAIN_MOON_BURNER_ETHEREUM_SEPOLIA:-0x47240bE29d50Eeb46bCCE0c227D67A34CE18682c}
        CHAIN_V3_CONCENTRATOR_ETHEREUM_SEPOLIA: ${CHAIN_V3_CONCENTRATOR_ETHEREUM_SEPOLIA:-0x17Fa6827FacD0B41Fa263ed1bC1E6D0bD73DaD30}
        CHAIN_MOON_TOKEN_ETHEREUM_SEPOLIA: ${CHAIN_MOON_TOKEN_ETHEREUM_SEPOLIA:-0x4E0994001515F4fe9335bbE73691750859a82835}
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    ports:
      - "8080:80"

volumes:
  db-data:
